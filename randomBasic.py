"""
random: this library is use to select a random no from a range or from an array.

library to use
random

functions to use:
    choice: this function pick a random no from a given array
    randint: returns a random no from a range
    randrange: returns a random no from a range
    

import random
a=[6,4,5,67,7,7,5,32,54,67]
z=random.choice(a)
print(z)
"""
import random
z=random.randint(0,1000)
print(z)




