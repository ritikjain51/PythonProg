"""
matplotlib: this library is use for plotting. supported by matlab.

library to use:
matplotlib

functions to use:
    title: this is use to set the title of a graph
    xlabel: this sets the x axis label name
    ylabel: this sets the y axis label name
    xlim: it limits the scale at x axis
    ylim: it limits the scale at y axis
    plot: this function helps in making a plot with two values ie x any y values.
    scatter: this also makes plot but we can add more properties in this function
    show: this function show the above made plot. no changes in the plot can done after this function
    

"""
import matplotlib.py
plt.title('churn_modelling')
plt.xlabel("surename")
plt.ylabel("balance")

#plt.plot(a,b,'o',color='red')
#plt.xlim(0,10)
plt.show()